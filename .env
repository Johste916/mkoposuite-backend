# ───────────────────────
# 🌍 Production (Supabase)
# ───────────────────────
# Only read when NODE_ENV=production by your sequelize config.
DATABASE_URL=postgresql://postgres.wmqicrqpffbcosxvkfci:Johsta67!@aws-0-eu-central-1.pooler.supabase.com:6543/postgres

# For Supabase/pgbouncer production you typically want SSL:
# PGSSLMODE=require
# DB_SSL=true
# (leave disabled while developing locally)
PGSSLMODE=disable
DB_SSL=false

# ───────────────────────
# 🖥️ Local Development DB
# ───────────────────────
DB_USER=postgres
DB_PASS=Johsta67!
DB_NAME=johsta_lms
DB_HOST=127.0.0.1
DB_PORT=5432

# ───────────────────────
# 🔁 Multitenancy
# ───────────────────────
MULTI_TENANT_MODE=optional
DEFAULT_TENANT_ID=00000000-0000-0000-0000-000000000000

# Optional helpers used by signup/tenants create (safe to keep)
DEFAULT_COUNTRY=TZ
DEFAULT_CURRENCY=TZS

# ───────────────────────
# 🔐 Auth + Frontend/CORS
# ───────────────────────
JWT_SECRET=mkoposuite_secure_2024
JWT_EXPIRES_IN=7d
PORT=10000
FRONTEND_URL=https://strong-fudge-7fc28d.netlify.app
CORS_ALLOW_ORIGINS=http://localhost:5173,https://strong-fudge-7fc28d.netlify.app

# Optional: rate limit override (keeps existing default of 120 if unset)
RATE_LIMIT_MAX=120

# ───────────────────────
# ⚙️ Runtime
# ───────────────────────
NODE_ENV=development
TZ=Africa/Nairobi

# ───────────────────────
# 🧰 Optional one-off boot syncs
# ───────────────────────
SYNC_SETTINGS_ONLY=false
SYNC_ACL=false
SYNC_AUDIT=false
SYNC_SAVINGS=false

# ───────────────────────
# 🆕 Self-service signup (public /api/signup)
# ───────────────────────
SELF_SIGNUP_ENABLED=1
DEFAULT_TRIAL_DAYS=14
REQUIRE_EMAIL_VERIFICATION=0

# (Optional email for verification/reset)
# SMTP_HOST=smtp.yourprovider.com
# SMTP_PORT=587
# SMTP_USER=apikey-or-username
# SMTP_PASS=super-secret
# SMTP_FROM="MkopoSuite <no-reply@yourdomain.com>"

# (Optional: if you add captcha to signup later)
# RECAPTCHA_SECRET=...

# ───────────────────────
# ✉️  SMS — sms.co.tz integration
# ───────────────────────
# Choose ONE auth method:
# 1) API key (if you later get one)
# SMS_CO_TZ_API_KEY=
# 2) or username/password (your current creds)
SMS_CO_TZ_USERNAME=TIMIZA MAONO
SMS_CO_TZ_PASSWORD=timizamaono2019

# Provider base
SMS_CO_TZ_API_BASE=https://www.sms.co.tz/api.php

# Default sender ID (must be approved at sms.co.tz)
SMS_CO_TZ_SENDER_DEFAULT=TIMIZAMAONO

# Optional per-tenant mapping (JSON)
SMS_SENDER_MAP_JSON={"00000000-0000-0000-0000-000000000000":"TIMIZAMAONO"}

# Allow clients to override sender via request body? (usually keep FALSE)
SMS_ALLOW_BODY_SENDERID=false

# Rate and testing
SMS_RATE_PER_MIN=60
SMS_DRY_RUN=0

# Country code used by normalizer (accepts +255 or 255)
DEFAULT_COUNTRY_CODE=+255
