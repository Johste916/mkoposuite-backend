# 🌍 Production (Supabase)
# If you use the Supabase connection pooler (6543), keep SSL on.
DATABASE_URL=postgresql://postgres.wmqicrqpffbcosxvkfci:Johsta67!@aws-0-eu-central-1.pooler.supabase.com:6543/postgres
PGSSLMODE=require

# 🖥️ Local Development
DB_USER=postgres
DB_PASS=Johsta67!
DB_NAME=johsta_lms
DB_HOST=127.0.0.1
DB_PORT=5432

# 🔁 Multitenancy
# off       -> ignore tenant (single-tenant behavior)
# optional  -> use x-tenant-id if present, else fallback to DEFAULT_TENANT_ID
# enforced  -> writes require a tenant; reads may still work if your code allows
MULTI_TENANT_MODE=optional
DEFAULT_TENANT_ID=00000000-0000-0000-0000-000000000000

# 🔐 Auth + Frontend
JWT_SECRET=mkoposuite_secure_2024
JWT_EXPIRES_IN=7d
PORT=10000
FRONTEND_URL=https://strong-fudge-7fc28d.netlify.app
# Comma-separated list for CORS if you also run locally:
CORS_ALLOW_ORIGINS=http://localhost:5173,https://strong-fudge-7fc28d.netlify.app

# ⚙️ Runtime
NODE_ENV=development
# Set your preferred server timezone (affects Date.now() → logs, day boundaries, etc.)
TZ=Africa/Nairobi

# 🧰 Optional one-off boot syncs (all default false; enable only when needed)
# SYNC_SETTINGS_ONLY=true   -> seed/sync system settings
# SYNC_ACL=true             -> seed roles/permissions
# SYNC_AUDIT=true           -> create/alter audit_logs
# SYNC_SAVINGS=true         -> create/alter savings tables
SYNC_SETTINGS_ONLY=false
SYNC_ACL=false
SYNC_AUDIT=false
SYNC_SAVINGS=false
